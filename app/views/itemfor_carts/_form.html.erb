<%= form_with(model: itemfor_cart, local: true) do |form| %>
  <% if itemfor_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(itemfor_cart.errors.count, "error") %> prohibited this itemfor_cart from being saved:</h2>

      <ul>
        <% itemfor_cart.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :chocolate_bar_id %>
    <%= form.text_field :chocolate_bar_id %>
  </div>

  <div class="field">
    <%= form.label :cart_id %>
    <%= form.text_field :cart_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
