<%= form_with(model: product_cart, local: true) do |form| %>
  <% if product_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_cart.errors.count, "error") %> prohibited this product_cart from being saved:</h2>

      <ul>
        <% product_cart.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
